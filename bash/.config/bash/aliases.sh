# Personal shell aliases. Sourced from ~/.bashrc by dotfiles install.sh.
# Aliases here beat any bashmarks functions of the same name (alias
# expansion happens before function resolution).

alias p='pnpm'
